# ============================================================================
# Nginx configuration for MarineSABRES DA Tool - DEVELOPMENT VERSION
# Direct port access at http://laguna.ku.lt:5003
# ============================================================================

# Development version - direct port 5003 access
location /da-dev/ {
    # Optional: redirect to port 5003
    return 301 $scheme://$server_name:5003$request_uri;
}

# Note: For direct port access (http://laguna.ku.lt:5003),
# no nginx configuration is needed - the application binds directly to 0.0.0.0:5003
# This file is kept for reference and optional subdirectory deployment

# If you want to deploy development version at /da-dev/ subpath instead:
# 1. Change gunicorn.conf.py to bind to 127.0.0.1:5003
# 2. Add APPLICATION_ROOT=/da-dev to raw_env in gunicorn.conf.py
# 3. Uncomment the configuration below:

# location /da-dev/static/ {
#     alias /home/razinka/OneDrive/HORIZON_EUROPE/Marine-SABRES/DAViewer-dev/static/;
#     expires 1h;  # Shorter cache for development
#     add_header Cache-Control "public";
# }

# location /da-dev/ {
#     rewrite ^/da-dev(/.*)$ $1 break;
#     proxy_pass http://127.0.0.1:5003;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Prefix /da-dev;
#     proxy_set_header X-Script-Name /da-dev;
#     proxy_set_header X-Forwarded-Proto $scheme;
#
#     # WebSocket support (if needed)
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#
#     # Timeouts
#     proxy_connect_timeout 60s;
#     proxy_send_timeout 60s;
#     proxy_read_timeout 60s;
# }

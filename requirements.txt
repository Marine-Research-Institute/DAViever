# Core Flask dependencies - Updated for security and performance
Flask==3.1.2  # Latest stable (Aug 2025)
Flask-Caching==2.3.1  # Latest stable (Feb 2025)
Flask-Limiter==4.0.0  # API rate limiting for security (Sep 2025) - Requires Python >=3.10
requests==2.32.5  # Latest stable (Aug 2025)
Werkzeug>=3.1.0  # Explicit Flask dependency for security

# Production deployment dependencies
gunicorn>=23.0.0  # WSGI HTTP server for production (Latest Oct 2025)
redis>=6.4.0  # Optional: Redis for distributed caching (Latest 2025)